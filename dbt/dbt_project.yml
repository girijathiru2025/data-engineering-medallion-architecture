name: 'de_medallion_etl'
version: '1.0.0'
config-version: 2

profile: 'de_medallion_etl'

model-paths: ["models"]
test-paths:  ["tests"]
seed-paths:  ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  medallion_etl:
    gold:
      +schema: gold
      +materialized: table
